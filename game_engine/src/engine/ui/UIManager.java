package engine.ui;

import java.awt.Graphics2D;

/**
 * UIManager handles all active UI components (buttons, text fields, overlays).
 */
public class UIManager {
    
    public UIManager() {
        // Initialize UI components list
    }

    public void update() {
        // Handle logic like hover states, clicks, etc.
    }

    public void render(Graphics2D g) {
        // Render UI over the game world
    }
    
    // Add methods to add/remove buttons, text, etc.
}
